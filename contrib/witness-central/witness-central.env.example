# Lucairn kit — `witness-central` topology, environment set
#
# Copy to `contrib/witness-central/witness-central.env` and fill in. Pass it as
# a SECOND --env-file after customer.env; nothing here overlaps customer.env, so
# the stock install is unaffected when this overlay is not applied.
#
# Full procedure, including the CA ceremony that produces the files referenced
# below: docs/WITNESS_CENTRAL_RUNBOOK.md

# ── Required ────────────────────────────────────────────────────────

# host:port of the CENTRAL witness's claim-intake port. This is the address the
# gateway, sanitizer, id-bridge and audit emitters dial.
#
# Reachability check before you start the stack:
#   nc -vz <host> 50057
#
# Note this is a gRPC port, not HTTP — a browser or plain `curl` will not give a
# meaningful answer, and "connection refused" here is a firewall/NAT problem,
# not a Lucairn one.
LUCAIRN_CENTRAL_WITNESS_ADDR=witness.example.eu:50057

# Host directory holding THIS DEVICE's client credential. Must contain exactly:
#
#   ca.pem      the shared CA bundle that issued the central witness's server
#               leaf (NOT a public root store — see the runbook on why)
#   client.pem  this device's client leaf, issued by that CA
#   client.key  its private key, mode 0600, never shared between devices
#
# Mounted read-only at /etc/lucairn/witness-client in every emitter container and
# wired via the WITNESS-SCOPED LCR_WITNESS_MTLS_* variables — deliberately not
# the mesh-wide DSA_MTLS_* ones, which are process-wide and would also force
# every local dial (gateway→audit, →id-bridge, →inference, sanitizer→reid-guard)
# onto mTLS against peers that are still plaintext.
#
# This is the CLAIM hop (:50057) only. The certificate hop has its own directory
# and its own leaf — see LUCAIRN_WITNESS_GATEWAY_CLIENT_CERT_DIR below.
LUCAIRN_WITNESS_CLIENT_CERT_DIR=/opt/lucairn/certs/witness-client

# Host directory holding the GATEWAY's credential for the CERTIFICATE port
# (:50058). A DIFFERENT directory, holding a DIFFERENT leaf and a DIFFERENT
# private key from the one above. Must contain exactly:
#
#   ca.pem      the same CA bundle as above (one CA, two leaves)
#   client.pem  a client leaf whose Subject CN is EXACTLY `gateway`
#   client.key  its private key, mode 0600
#
# Mounted read-only at /etc/lucairn/witness-gateway-client into the gateway
# container and NOWHERE ELSE, and wired via the WITNESS_MTLS_* family.
#
# ⚠️ WHY NOT JUST REUSE THE DIRECTORY ABOVE. Because a shared identity makes
# per-method authorization unenforceable, and both halves of that failure are
# easy to walk into:
#
#   - The device leaf is CN=lucairn-device-<name>. The central witness's :50058
#     ACL admits CN `gateway` and nothing else, so pointing the gateway at the
#     device credential simply breaks certificate retrieval and /verify.
#   - Repairing that by adding the device CN to the witness's export/cert
#     allowlists grants it to EVERY container that mounts the same private key —
#     audit, id-bridge and the sanitizer, which holds raw PII. Each of them
#     could then call ExportCertificates and stream out placeholder→original
#     maps, with the mTLS latch fully engaged. The witness can only tell apart
#     callers that present different certificates.
#
# One leaf per role, one mount per role. The claim hop keeps the device
# identity because that is genuinely what submits the claim.
#
# There is deliberately NO default: an unset value fails the compose render
# naming this variable. See docs/WITNESS_CENTRAL_RUNBOOK.md §3.4.
LUCAIRN_WITNESS_GATEWAY_CLIENT_CERT_DIR=/opt/lucairn/certs/witness-gateway-client

# ── Optional ────────────────────────────────────────────────────────

# The mTLS latch. Leave at "true".
#
# Setting it to "false" disengages the fail-closed behaviour: an incomplete or
# missing credential then falls back to a CLEARTEXT dial to
# LUCAIRN_CENTRAL_WITNESS_ADDR, which puts the sanitizer's
# redaction_manifest_body — the raw placeholder→original map — on the public
# internet with no warning in any log.
#
# ⚠️ STRICT VALUE GRAMMAR. `true` and `false` (and empty, meaning false) are the
# only accepted values; ANY OTHER VALUE STOPS THE PROCESS naming this variable.
# It is not a truthiness test. Before 2026-07-28 "1", "yes" and a mistyped
# "ture" all read as DISENGAGED, so a typo here produced the cleartext dial
# described above while this file said the boundary was on.
#
# There is no legitimate reason to turn this off in a witness-central
# deployment. It exists as a knob only so the same images can still run the
# stock LAN topology.
LCR_WITNESS_REQUIRE_MTLS=true

# ── Required (cont.) ────────────────────────────────────────────────

# Certificate RETRIEVAL address (:50058), a different port with a different
# credential family (WITNESS_MTLS_*).
#
# REQUIRED, not optional. An earlier revision of this overlay let it default to
# the local `veil-witness:50058`. The laptop no longer runs a witness, so there
# is nothing local to fall back to: leaving this unset now fails the compose
# render with a sentence naming this variable, which is a better outcome than
# the gateway's /verify and /summary surfaces failing at request time with a
# connection error.
#
# The gateway's WITNESS_MTLS_* CLIENT triple is wired by the overlay from
# LUCAIRN_WITNESS_GATEWAY_CLIENT_CERT_DIR — its OWN directory, holding its OWN
# CN=gateway leaf. You do not set WITNESS_MTLS_* by hand, but you DO have to
# provision that second directory; it is not a copy of the claim-hop one.
#
# ⚠️ Without a client triple the cert server has NO client authentication and NO
# authorization, and GetCertificate/ExportCertificates return certificates
# carrying redaction_manifest_body. The runbook's ingress guidance is to publish
# :50057 broadly and :50058 per-device.
LUCAIRN_CENTRAL_WITNESS_CERT_ADDR=witness.example.eu:50058

# ── Optional (cont.) ────────────────────────────────────────────────

# Override the TLS ServerName the gateway verifies on the :50058 hop. Set this
# when the address you publish differs from the SAN the CA ceremony issued on
# the central witness's server leaf — otherwise the handshake fails with a
# hostname mismatch that reads like a certificate problem.
# LUCAIRN_CENTRAL_WITNESS_SERVER_NAME=witness

# ── MANDATORY MANUAL STEP (full on-prem only): sandbox-b's credential ──
#
# Applies ONLY if you run `-f docker-compose.customer.yml -f
# docker-compose.self-hosted.yml` — the full on-prem set — together with this
# overlay. On the customer compose alone, sandbox-b is not in your project and
# there is nothing to do.
#
# sandbox-b is the AI plane's claim emitter. It is defined only in
# docker-compose.self-hosted.yml, and this overlay cannot add a `sandbox-b:`
# block to it: the overlay is also applied WITHOUT that file, where such a block
# would create an imageless service and break the render.
#
# Its ADDRESS needs nothing from you — docker-compose.self-hosted.yml reads
# LUCAIRN_CENTRAL_WITNESS_ADDR above, defaulting to the local
# `veil-witness:50057` when it is unset.
#
# Its CREDENTIAL does. LCR_WITNESS_MTLS_SANDBOX_B_* and the device-wide
# LCR_WITNESS_MTLS_* fallback both resolve to the EMPTY STRING by default, and
# no witness client-credential directory is mounted into that container (this
# overlay mounts it into audit, id-bridge, sanitizer and gateway only). With
# LCR_WITNESS_REQUIRE_MTLS=true above, sandbox-b therefore holds the latch with
# nothing to satisfy it. That must fail LOUD at boot — never a cleartext dial,
# never a quietly PARTIAL certificate.
#
# Provision it yourself, using the SAME §3.3 device claim leaf the other
# emitters use (one device, one claim identity — do not mint a second):
#
#   1. mount it read-only into sandbox-b, in docker-compose.self-hosted.yml or
#      an overlay of your own applied after it:
#
#        services:
#          sandbox-b:
#            volumes:
#              - /opt/lucairn/certs/witness-client:/etc/lucairn/witness-client:ro
#
#   2. set these in customer.env (NOT here — they belong to the base install's
#      variable set, and this file is applied as a second --env-file):
#
#        LCR_WITNESS_MTLS_SANDBOX_B_CA_BUNDLE_PATH=/etc/lucairn/witness-client/ca.pem
#        LCR_WITNESS_MTLS_SANDBOX_B_CLIENT_CERT_PATH=/etc/lucairn/witness-client/client.pem
#        LCR_WITNESS_MTLS_SANDBOX_B_CLIENT_KEY_PATH=/etc/lucairn/witness-client/client.key
#
#   3. verify BOTH halves in the render before starting anything — see
#      docs/WITNESS_CENTRAL_RUNBOOK.md § 7b for the exact jq command. Env paths
#      without the mount is a file-not-found at boot; the mount without the env
#      paths is a credential sitting unused in a container.
#
# ⚠️ No default bind mount is shipped for sandbox-b, deliberately. Docker
# materialises a bind mount of a non-existent host path as an EMPTY DIRECTORY
# rather than failing, so a shipped default would turn "no credential" into "a
# mount that looks present and reads empty".

# ── ⚠️ THE DEV-ONLY PROFILE RESTORES SELF-SIGNED EVIDENCE ────────────
#
# `--profile witness-local-dev-only` starts the local witness again. It is the
# flag you will find first if a compose error sends you searching, and it is
# almost never the right answer:
#
# Activating it means this host signs the certificates that attest to its own
# operator's conduct, with the witness signing key on this machine, reaching no
# TSA and no Rekor (`anchor_status` stays `pending` forever). OFFLINE DEMOS
# ONLY. Never a pilot or production topology.
#
# The same text is attached to the service as the compose label
# `eu.lucairn.witness.local-profile.warning`, so it shows up in
# `docker compose config` and `docker inspect` once the profile is on.
#
# The two errors it appears to fix have correct repairs instead:
#   - `cert-builder depends on undefined service "veil-witness"` — you combined
#     `--profile certification` with this overlay. Don't: cert-builder DRIVES a
#     witness rather than dialling one and genuinely needs one locally. Run the
#     certification profile on the stock topology, without this overlay.
#   - `sandbox-b depends on undefined service "veil-witness"` — already fixed;
#     that edge is `required: false`. Seeing it means your
#     docker-compose.self-hosted.yml predates 2026-07-28. Update the kit.
#
# See docs/WITNESS_CENTRAL_RUNBOOK.md § 7c.

# ── MANDATORY MANUAL STEP: retire the local witness signing key ─────
#
# `bin/lucairn init` generates LCR_WITNESS_SIGNING_KEY (and its VEIL_ alias)
# into customer.env for every topology. This overlay cannot reach into that
# generator, so the key is still sitting in your customer.env after you adopt
# witness-central — unused, but present.
#
# The PRD's success criterion is that the laptop holds NO signing key in the
# witness role. "Unused" is not "absent": a key on the machine is a key that can
# be used, and the whole point of a central witness is that the operator cannot
# sign evidence about their own conduct.
#
# After your first successful witness-central turn, delete these lines from
# customer.env and recreate the stack:
#
#   LCR_WITNESS_SIGNING_KEY=...
#   VEIL_WITNESS_SIGNING_KEY=...
#
# Keep LCR_WITNESS_PUBLIC_KEY — the gateway publishes it at
# /.well-known/veil-keys.json and verifiers need it. Replace its VALUE with the
# CENTRAL witness's public key, or the discovery surface will advertise a key
# that signed nothing.
#
# See docs/WITNESS_CENTRAL_RUNBOOK.md § "Retire the local signing key".
