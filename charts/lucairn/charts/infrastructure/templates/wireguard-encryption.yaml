{{- /*
WireGuard annotations belong to the canonical Namespace objects in
namespaces.yaml. Keeping this template intentionally resource-free prevents a
WireGuard overlay from emitting a second Namespace identity.
*/ -}}
