# Disposable Kind-only API admission fixtures. These permissive structural CRDs
# admit the chart's external-secrets.io/v1beta1 resources without installing an
# External Secrets Operator. The real harness waits only for Established; it
# never treats custom-resource status as reconciliation evidence.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: externalsecrets.external-secrets.io
spec:
  group: external-secrets.io
  scope: Namespaced
  names:
    plural: externalsecrets
    singular: externalsecret
    kind: ExternalSecret
    listKind: ExternalSecretList
  versions:
    - name: v1beta1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          x-kubernetes-preserve-unknown-fields: true
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: clustersecretstores.external-secrets.io
spec:
  group: external-secrets.io
  scope: Cluster
  names:
    plural: clustersecretstores
    singular: clustersecretstore
    kind: ClusterSecretStore
    listKind: ClusterSecretStoreList
  versions:
    - name: v1beta1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          x-kubernetes-preserve-unknown-fields: true
